/*
 * مشروع: إضاءة LED وامضة
 * الوصف: هذا المشروع يقوم بإضاءة وإطفاء LED بشكل متكرر
 * المكونات المطلوبة:
 *   - Arduino Uno
 *   - LED واحد
 *   - مقاومة 220 أوم
 *   - أسلاك توصيل
 * 
 * التوصيلات:
 *   - الطرف الموجب للـ LED (الطرف الطويل) → مقاومة 220 أوم → Pin 13
 *   - الطرف السالب للـ LED (الطرف القصير) → GND
 */

// تحديد رقم المنفذ الذي سيتم توصيل LED به
const int ledPin = 13;

void setup() {
  // إعداد المنفذ كمخرج
  pinMode(ledPin, OUTPUT);
}

void loop() {
  // إضاءة LED
  digitalWrite(ledPin, HIGH);
  // الانتظار لمدة ثانية واحدة (1000 ميلي ثانية)
  delay(1000);
  
  // إطفاء LED
  digitalWrite(ledPin, LOW);
  // الانتظار لمدة ثانية واحدة
  delay(1000);
}

/*
 * شرح الكود:
 * - setup(): تعمل مرة واحدة عند تشغيل البرنامج، نحدد فيها المنفذ 13 كمخرج
 * - loop(): تتكرر باستمرار، تقوم بإضاءة LED لثانية ثم إطفائه لثانية
 * - digitalWrite(): تستخدم لإرسال إشارة HIGH (تشغيل) أو LOW (إيقاف)
 * - delay(): تستخدم للانتظار بالميلي ثانية
 */
